#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _puts - function that returns the length of a string.
 * @str: pointer to char
 * Return: void
 */

void _puts(char *str)
{
write(1, &c, 1);
}
