#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - program that prints the alphabet in lowercase, followed by a new line.
* Return: 0
*/
int main(void)
{
for (int i = 0; i < 10; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
